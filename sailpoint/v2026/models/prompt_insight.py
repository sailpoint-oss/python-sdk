# coding: utf-8

"""
    Identity Security Cloud v2026 API

    Use these APIs to interact with the Identity Security Cloud platform to achieve repeatable, automated processes with greater scalability. We encourage you to join the SailPoint Developer Community forum at https://developer.sailpoint.com/discuss to connect with other developers using our APIs.

    The version of the OpenAPI document: v2026
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json
import warnings

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class PromptInsight(BaseModel):
    """
    A prompt security insight event.
    """ # noqa: E501
    timestamp: Optional[datetime] = Field(default=None, description="Event time in UTC.")
    user: Optional[StrictStr] = Field(default=None, description="User identifier or display name.")
    agent: Optional[StrictStr] = Field(default=None, description="The AI agent that processed the prompt.")
    policy_decision: Optional[StrictStr] = Field(default=None, description="The policy decision applied to the prompt.", alias="policyDecision")
    category: Optional[StrictStr] = Field(default=None, description="The category of the prompt security finding.")
    severity: Optional[StrictStr] = Field(default=None, description="The severity of the prompt security finding.")
    reason: Optional[StrictStr] = Field(default=None, description="Human-readable or structured reason for the policy decision.")
    rule: Optional[StrictStr] = Field(default=None, description="The rule that matched the prompt.")
    policy: Optional[StrictStr] = Field(default=None, description="The policy that matched the prompt.")
    __properties: ClassVar[List[str]] = ["timestamp", "user", "agent", "policyDecision", "category", "severity", "reason", "rule", "policy"]

    @field_validator('policy_decision')
    def policy_decision_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['ALLOWED', 'REDACTED']):
            warnings.warn(f"must be one of enum values ('ALLOWED', 'REDACTED') unknown value: {value}")
        return value

    @field_validator('category')
    def category_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['ANOMALIES', 'DATA_UPLOADS', 'MCP_TOOL_CALLS']):
            warnings.warn(f"must be one of enum values ('ANOMALIES', 'DATA_UPLOADS', 'MCP_TOOL_CALLS') unknown value: {value}")
        return value

    @field_validator('severity')
    def severity_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['LOW', 'MEDIUM', 'HIGH', 'CRITICAL']):
            warnings.warn(f"must be one of enum values ('LOW', 'MEDIUM', 'HIGH', 'CRITICAL') unknown value: {value}")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PromptInsight from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PromptInsight from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "timestamp": obj.get("timestamp"),
            "user": obj.get("user"),
            "agent": obj.get("agent"),
            "policyDecision": obj.get("policyDecision"),
            "category": obj.get("category"),
            "severity": obj.get("severity"),
            "reason": obj.get("reason"),
            "rule": obj.get("rule"),
            "policy": obj.get("policy")
        })
        return _obj


