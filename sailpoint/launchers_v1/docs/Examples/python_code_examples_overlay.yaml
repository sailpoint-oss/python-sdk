- path: /launchers/v1
  method: POST
  xCodeSample:
    - lang: Python
      label: SDK_tools/sdk/python/v1/methods/launchers#create-launcher-v1
      source: |
        from sailpoint.launchers_v1.api.launchers_v1_api import LaunchersV1Api
        from sailpoint.launchers_v1.api_client import ApiClient
        from sailpoint.launchers_v1.models.launcher import Launcher
        from sailpoint.launchers_v1.models.launcherrequest import Launcherrequest
        from sailpoint.configuration import Configuration
        configuration = Configuration()


        with ApiClient(configuration) as api_client:
          launcherrequest = '''sailpoint.launchers_v1.Launcherrequest()''' # Launcherrequest | Payload to create a Launcher
          try:
              # Create launcher
              new_launcherrequest = Launcherrequest.from_json(launcherrequest)
              results = LaunchersV1Api(api_client).create_launcher_v1(launcherrequest=new_launcherrequest)
              # Below is a request that includes all optional parameters
              # results = LaunchersV1Api(api_client).create_launcher_v1(new_launcherrequest)
              print("The response of LaunchersV1Api->create_launcher_v1:\n")
              print(results.model_dump_json(by_alias=True, indent=4))
          except Exception as e:
              print("Exception when calling LaunchersV1Api->create_launcher_v1: %s\n" % e)
- path: /launchers/v1/{launcherID}
  method: DELETE
  xCodeSample:
    - lang: Python
      label: SDK_tools/sdk/python/v1/methods/launchers#delete-launcher-v1
      source: |
        from sailpoint.launchers_v1.api.launchers_v1_api import LaunchersV1Api
        from sailpoint.launchers_v1.api_client import ApiClient
        from sailpoint.configuration import Configuration
        configuration = Configuration()


        with ApiClient(configuration) as api_client:
          launcher_id = 'e3012408-8b61-4564-ad41-c5ec131c325b' # str | ID of the Launcher to be deleted # str | ID of the Launcher to be deleted
          try:
              # Delete launcher
              
              LaunchersV1Api(api_client).delete_launcher_v1(launcher_id=launcher_id)
              # Below is a request that includes all optional parameters
              # LaunchersV1Api(api_client).delete_launcher_v1(launcher_id)
          except Exception as e:
              print("Exception when calling LaunchersV1Api->delete_launcher_v1: %s\n" % e)
- path: /launchers/v1/{launcherID}
  method: GET
  xCodeSample:
    - lang: Python
      label: SDK_tools/sdk/python/v1/methods/launchers#get-launcher-v1
      source: |
        from sailpoint.launchers_v1.api.launchers_v1_api import LaunchersV1Api
        from sailpoint.launchers_v1.api_client import ApiClient
        from sailpoint.launchers_v1.models.launcher import Launcher
        from sailpoint.configuration import Configuration
        configuration = Configuration()


        with ApiClient(configuration) as api_client:
          launcher_id = 'e3012408-8b61-4564-ad41-c5ec131c325b' # str | ID of the Launcher to be retrieved # str | ID of the Launcher to be retrieved
          try:
              # Get launcher by id
              
              results = LaunchersV1Api(api_client).get_launcher_v1(launcher_id=launcher_id)
              # Below is a request that includes all optional parameters
              # results = LaunchersV1Api(api_client).get_launcher_v1(launcher_id)
              print("The response of LaunchersV1Api->get_launcher_v1:\n")
              print(results.model_dump_json(by_alias=True, indent=4))
          except Exception as e:
              print("Exception when calling LaunchersV1Api->get_launcher_v1: %s\n" % e)
- path: /launchers/v1
  method: GET
  xCodeSample:
    - lang: Python
      label: SDK_tools/sdk/python/v1/methods/launchers#get-launchers-v1
      source: |
        from sailpoint.launchers_v1.api.launchers_v1_api import LaunchersV1Api
        from sailpoint.launchers_v1.api_client import ApiClient
        from sailpoint.launchers_v1.models.get_launchers_v1200_response import GetLaunchersV1200Response
        from sailpoint.configuration import Configuration
        configuration = Configuration()


        with ApiClient(configuration) as api_client:
          filters = 'disabled eq \"true\"' # str | Filter results using the standard syntax described in [V3 API Standard Collection Parameters](https://developer.sailpoint.com/idn/api/standard-collection-parameters#filtering-results)  Filtering is supported for the following fields and operators:  **description**: *sw*  **disabled**: *eq*  **name**: *sw* (optional) # str | Filter results using the standard syntax described in [V3 API Standard Collection Parameters](https://developer.sailpoint.com/idn/api/standard-collection-parameters#filtering-results)  Filtering is supported for the following fields and operators:  **description**: *sw*  **disabled**: *eq*  **name**: *sw* (optional)
          next = 'eyJuZXh0IjoxMjN9Cg==' # str | Pagination marker (optional) # str | Pagination marker (optional)
          limit = 10 # int | Number of Launchers to return (optional) (default to 10) # int | Number of Launchers to return (optional) (default to 10)
          try:
              # List all launchers for tenant
              
              results = LaunchersV1Api(api_client).get_launchers_v1()
              # Below is a request that includes all optional parameters
              # results = LaunchersV1Api(api_client).get_launchers_v1(filters, next, limit)
              print("The response of LaunchersV1Api->get_launchers_v1:\n")
              print(results.model_dump_json(by_alias=True, indent=4))
          except Exception as e:
              print("Exception when calling LaunchersV1Api->get_launchers_v1: %s\n" % e)
- path: /launchers/v1/{launcherID}
  method: PUT
  xCodeSample:
    - lang: Python
      label: SDK_tools/sdk/python/v1/methods/launchers#put-launcher-v1
      source: |
        from sailpoint.launchers_v1.api.launchers_v1_api import LaunchersV1Api
        from sailpoint.launchers_v1.api_client import ApiClient
        from sailpoint.launchers_v1.models.launcher import Launcher
        from sailpoint.launchers_v1.models.launcherrequest import Launcherrequest
        from sailpoint.configuration import Configuration
        configuration = Configuration()


        with ApiClient(configuration) as api_client:
          launcher_id = 'e3012408-8b61-4564-ad41-c5ec131c325b' # str | ID of the Launcher to be replaced # str | ID of the Launcher to be replaced
          launcherrequest = '''sailpoint.launchers_v1.Launcherrequest()''' # Launcherrequest | Payload to replace Launcher
          try:
              # Replace launcher
              new_launcherrequest = Launcherrequest.from_json(launcherrequest)
              results = LaunchersV1Api(api_client).put_launcher_v1(launcher_id=launcher_id, launcherrequest=new_launcherrequest)
              # Below is a request that includes all optional parameters
              # results = LaunchersV1Api(api_client).put_launcher_v1(launcher_id, new_launcherrequest)
              print("The response of LaunchersV1Api->put_launcher_v1:\n")
              print(results.model_dump_json(by_alias=True, indent=4))
          except Exception as e:
              print("Exception when calling LaunchersV1Api->put_launcher_v1: %s\n" % e)
- path: /launchers/v1/{launcherID}/launch
  method: POST
  xCodeSample:
    - lang: Python
      label: SDK_tools/sdk/python/v1/methods/launchers#start-launcher-v1
      source: |
        from sailpoint.launchers_v1.api.launchers_v1_api import LaunchersV1Api
        from sailpoint.launchers_v1.api_client import ApiClient
        from sailpoint.launchers_v1.models.start_launcher_v1200_response import StartLauncherV1200Response
        from sailpoint.configuration import Configuration
        configuration = Configuration()


        with ApiClient(configuration) as api_client:
          launcher_id = 'e3012408-8b61-4564-ad41-c5ec131c325b' # str | ID of the Launcher to be launched # str | ID of the Launcher to be launched
          try:
              # Launch a launcher
              
              results = LaunchersV1Api(api_client).start_launcher_v1(launcher_id=launcher_id)
              # Below is a request that includes all optional parameters
              # results = LaunchersV1Api(api_client).start_launcher_v1(launcher_id)
              print("The response of LaunchersV1Api->start_launcher_v1:\n")
              print(results.model_dump_json(by_alias=True, indent=4))
          except Exception as e:
              print("Exception when calling LaunchersV1Api->start_launcher_v1: %s\n" % e)

