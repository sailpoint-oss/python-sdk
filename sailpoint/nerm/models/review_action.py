# coding: utf-8

"""
    NERM API

    The NERM API accesss and modifies resources in your environment.

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json
import warnings

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class ReviewAction(BaseModel):
    """
    ReviewAction
    """ # noqa: E501
    workflow_id: StrictStr = Field(description="The workflow the workflow action belongs to.")
    description: StrictStr = Field(description="The description of the workflow action.")
    page_id: Optional[StrictStr] = Field(default=None, description="The page the workflow action should render.")
    archived: Optional[StrictBool] = Field(default=False, description="If the workflow action is archived or not.")
    requires_comment: Optional[StrictBool] = Field(default=False, description="If the workflow action requires a comment or not.")
    __properties: ClassVar[List[str]] = ["workflow_id", "description", "page_id", "archived", "requires_comment"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ReviewAction from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ReviewAction from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "workflow_id": obj.get("workflow_id"),
            "description": obj.get("description"),
            "page_id": obj.get("page_id"),
            "archived": obj.get("archived") if obj.get("archived") is not None else False,
            "requires_comment": obj.get("requires_comment") if obj.get("requires_comment") is not None else False
        })
        return _obj


