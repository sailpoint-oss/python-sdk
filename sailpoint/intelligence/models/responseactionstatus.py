# coding: utf-8

"""
    Identity Security Cloud API - Intelligence

    Use these APIs to interact with the Identity Security Cloud platform to achieve repeatable, automated processes with greater scalability. We encourage you to join the SailPoint Developer Community forum at https://developer.sailpoint.com/discuss to connect with other developers using our APIs.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json
import warnings

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List
from typing import Optional, Set
from typing_extensions import Self

class Responseactionstatus(BaseModel):
    """
    Current tracked status of a response action. Target fields from the create request (identityType, identityId, accountIds, context) are not echoed here; they travel on the action event used to start the workflow(s). 
    """ # noqa: E501
    request_id: StrictStr = Field(description="Tracking handle and correlation id for the response action.", alias="requestId")
    action_type: StrictStr = Field(description="The action that was requested.", alias="actionType")
    status: StrictStr = Field(description="Aggregate status across the correlated workflow execution(s): SUBMITTED (registered, no execution yet), IN_PROGRESS (any still non-terminal), COMPLETED (all terminal and at least one succeeded), or FAILED (all terminal and none succeeded). ")
    submitted_at: datetime = Field(description="When the response action was accepted.", alias="submittedAt")
    updated_at: datetime = Field(description="When the response action status last changed.", alias="updatedAt")
    __properties: ClassVar[List[str]] = ["requestId", "actionType", "status", "submittedAt", "updatedAt"]

    @field_validator('action_type')
    def action_type_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['DISABLE_IDENTITY', 'DISABLE_ACCOUNT']):
            warnings.warn(f"must be one of enum values ('DISABLE_IDENTITY', 'DISABLE_ACCOUNT') unknown value: {value}")
        return value

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['SUBMITTED', 'IN_PROGRESS', 'COMPLETED', 'FAILED']):
            warnings.warn(f"must be one of enum values ('SUBMITTED', 'IN_PROGRESS', 'COMPLETED', 'FAILED') unknown value: {value}")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Responseactionstatus from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Responseactionstatus from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "requestId": obj.get("requestId"),
            "actionType": obj.get("actionType"),
            "status": obj.get("status"),
            "submittedAt": obj.get("submittedAt"),
            "updatedAt": obj.get("updatedAt")
        })
        return _obj


