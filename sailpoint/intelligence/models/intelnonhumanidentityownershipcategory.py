# coding: utf-8

"""
    Identity Security Cloud API - Intelligence

    Use these APIs to interact with the Identity Security Cloud platform to achieve repeatable, automated processes with greater scalability. We encourage you to join the SailPoint Developer Community forum at https://developer.sailpoint.com/discuss to connect with other developers using our APIs.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json
import warnings

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from sailpoint.intelligence.models.intelnonhumanidentityownedslice import Intelnonhumanidentityownedslice
from typing import Optional, Set
from typing_extensions import Self

class Intelnonhumanidentityownershipcategory(BaseModel):
    """
    Ownership category for agents or applications. On success, primaryOwned and secondaryOwned carry independently paged buckets. On category-level upstream failure, message and reason are set (reason: UPSTREAM_UNAVAILABLE). The service emits one flat object today, so primaryOwned and secondaryOwned may still appear on the failure path with empty items. 
    """ # noqa: E501
    primary_owned: Optional[Intelnonhumanidentityownedslice] = Field(default=None, description="First page of non-human identities for which this human is the primary owner.", alias="primaryOwned")
    secondary_owned: Optional[Intelnonhumanidentityownedslice] = Field(default=None, description="First page of non-human identities for which this human is a secondary owner.", alias="secondaryOwned")
    message: Optional[StrictStr] = Field(default=None, description="Human-readable explanation of the temporary ownership data failure.")
    reason: Optional[StrictStr] = Field(default=None, description="Machine-readable reason code for the category-level ownership failure.")
    __properties: ClassVar[List[str]] = ["primaryOwned", "secondaryOwned", "message", "reason"]

    @field_validator('reason')
    def reason_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['UPSTREAM_UNAVAILABLE']):
            warnings.warn(f"must be one of enum values ('UPSTREAM_UNAVAILABLE') unknown value: {value}")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Intelnonhumanidentityownershipcategory from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of primary_owned
        if self.primary_owned:
            _dict['primaryOwned'] = self.primary_owned.to_dict()
        # override the default output from pydantic by calling `to_dict()` of secondary_owned
        if self.secondary_owned:
            _dict['secondaryOwned'] = self.secondary_owned.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Intelnonhumanidentityownershipcategory from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "primaryOwned": Intelnonhumanidentityownedslice.from_dict(obj["primaryOwned"]) if obj.get("primaryOwned") is not None else None,
            "secondaryOwned": Intelnonhumanidentityownedslice.from_dict(obj["secondaryOwned"]) if obj.get("secondaryOwned") is not None else None,
            "message": obj.get("message"),
            "reason": obj.get("reason")
        })
        return _obj


