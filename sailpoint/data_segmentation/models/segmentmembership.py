# coding: utf-8

"""
    Identity Security Cloud API - Data Segmentation

    Use these APIs to interact with the Identity Security Cloud platform to achieve repeatable, automated processes with greater scalability. We encourage you to join the SailPoint Developer Community forum at https://developer.sailpoint.com/discuss to connect with other developers using our APIs.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json
import warnings

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from sailpoint.data_segmentation.models.scopetype import Scopetype
from typing import Optional, Set
from typing_extensions import Self

class Segmentmembership(BaseModel):
    """
    Contains the segments and types that an identity is associated with
    """ # noqa: E501
    segments: Optional[List[StrictStr]] = Field(default=None, description="List of segment ids that the identity is associated with.")
    all_access_scopes: Optional[List[Union[Scopetype, str]]] = Field(default=None, description="They type of scopes that are assigned to the identity.", alias="allAccessScopes")
    refresh_by: Optional[datetime] = Field(default=None, description="Date time string that lets you know when the membership data is going to be refreshed.", alias="refreshBy")
    __properties: ClassVar[List[str]] = ["segments", "allAccessScopes", "refreshBy"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Segmentmembership from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Segmentmembership from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "segments": obj.get("segments"),
            "allAccessScopes": obj.get("allAccessScopes"),
            "refreshBy": obj.get("refreshBy")
        })
        return _obj


