# coding: utf-8

"""
    Identity Security Cloud API - Machine Identities

    Use these APIs to interact with the Identity Security Cloud platform to achieve repeatable, automated processes with greater scalability. We encourage you to join the SailPoint Developer Community forum at https://developer.sailpoint.com/discuss to connect with other developers using our APIs.

    The version of the OpenAPI document: v1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json
import warnings

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from sailpoint.machine_identities.models.correlation_condition import CorrelationCondition
from sailpoint.machine_identities.models.correlation_rule_action import CorrelationRuleAction
from typing import Optional, Set
from typing_extensions import Self

class CorrelationRule(BaseModel):
    """
    A single correlation rule within an ownership correlation config.
    """ # noqa: E501
    id: Optional[StrictStr] = Field(default=None, description="Omit for new rules (server mints a UUID). Send only when updating a rule that already exists on this config (merge on PATCH). Unknown ids are rejected.")
    priority: StrictInt = Field(description="The evaluation priority of the rule. Lower values are evaluated first.")
    default_rule: StrictBool = Field(description="Whether this rule is the default rule for the config.", alias="defaultRule")
    rule_type: StrictStr = Field(description="The rule subject type. When either ruleType or ruleAction.type is GOVERNANCE_GROUP, both must be; ruleType GOVERNANCE_GROUP is allowed only when the parent config type is OWNER_SECONDARY.", alias="ruleType")
    rule_action: CorrelationRuleAction = Field(alias="ruleAction")
    condition_expressions: List[CorrelationCondition] = Field(description="The conditions that must match for this rule to apply.", alias="conditionExpressions")
    __properties: ClassVar[List[str]] = ["id", "priority", "defaultRule", "ruleType", "ruleAction", "conditionExpressions"]

    @field_validator('rule_type')
    def rule_type_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['IDENTITY', 'ACCOUNT', 'GOVERNANCE_GROUP']):
            warnings.warn(f"must be one of enum values ('IDENTITY', 'ACCOUNT', 'GOVERNANCE_GROUP') unknown value: {value}")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CorrelationRule from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of rule_action
        if self.rule_action:
            _dict['ruleAction'] = self.rule_action.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in condition_expressions (list)
        _items = []
        if self.condition_expressions:
            for _item_condition_expressions in self.condition_expressions:
                if _item_condition_expressions:
                    _items.append(_item_condition_expressions.to_dict())
            _dict['conditionExpressions'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CorrelationRule from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "priority": obj.get("priority"),
            "defaultRule": obj.get("defaultRule"),
            "ruleType": obj.get("ruleType"),
            "ruleAction": CorrelationRuleAction.from_dict(obj["ruleAction"]) if obj.get("ruleAction") is not None else None,
            "conditionExpressions": [CorrelationCondition.from_dict(_item) for _item in obj["conditionExpressions"]] if obj.get("conditionExpressions") is not None else None
        })
        return _obj


