- path: /auth-profiles/v1
  method: GET
  xCodeSample:
    - lang: Python
      label: SDK_tools/sdk/python/v1/methods/auth-profile#get-profile-config-list-v1
      source: |
        from sailpoint.auth_profile_v1.api.auth_profile_v1_api import AuthProfileV1Api
        from sailpoint.auth_profile_v1.api_client import ApiClient
        from sailpoint.auth_profile_v1.models.authprofilesummary import Authprofilesummary
        from sailpoint.configuration import Configuration
        configuration = Configuration()

        configuration.experimental = True

        with ApiClient(configuration) as api_client:
          x_sail_point_experimental = 'true' # str | Use this header to enable this experimental API. (optional) (default to 'true') # str | Use this header to enable this experimental API. (optional) (default to 'true')
          try:
              # Get list of auth profiles
              
              results = AuthProfileV1Api(api_client).get_profile_config_list_v1()
              # Below is a request that includes all optional parameters
              # results = AuthProfileV1Api(api_client).get_profile_config_list_v1(x_sail_point_experimental)
              print("The response of AuthProfileV1Api->get_profile_config_list_v1:\n")
              for item in results:
                  print(item.model_dump_json(by_alias=True, indent=4))
          except Exception as e:
              print("Exception when calling AuthProfileV1Api->get_profile_config_list_v1: %s\n" % e)
- path: /auth-profiles/v1/{id}
  method: GET
  xCodeSample:
    - lang: Python
      label: SDK_tools/sdk/python/v1/methods/auth-profile#get-profile-config-v1
      source: |
        from sailpoint.auth_profile_v1.api.auth_profile_v1_api import AuthProfileV1Api
        from sailpoint.auth_profile_v1.api_client import ApiClient
        from sailpoint.auth_profile_v1.models.authprofile import Authprofile
        from sailpoint.configuration import Configuration
        configuration = Configuration()

        configuration.experimental = True

        with ApiClient(configuration) as api_client:
          id = '2c91808a7813090a017814121919ecca' # str | ID of the Auth Profile to patch. # str | ID of the Auth Profile to patch.
          x_sail_point_experimental = 'true' # str | Use this header to enable this experimental API. (optional) (default to 'true') # str | Use this header to enable this experimental API. (optional) (default to 'true')
          try:
              # Get auth profile
              
              results = AuthProfileV1Api(api_client).get_profile_config_v1(id=id)
              # Below is a request that includes all optional parameters
              # results = AuthProfileV1Api(api_client).get_profile_config_v1(id, x_sail_point_experimental)
              print("The response of AuthProfileV1Api->get_profile_config_v1:\n")
              print(results.model_dump_json(by_alias=True, indent=4))
          except Exception as e:
              print("Exception when calling AuthProfileV1Api->get_profile_config_v1: %s\n" % e)
- path: /auth-profiles/v1/{id}
  method: PATCH
  xCodeSample:
    - lang: Python
      label: SDK_tools/sdk/python/v1/methods/auth-profile#patch-profile-config-v1
      source: |
        from sailpoint.auth_profile_v1.api.auth_profile_v1_api import AuthProfileV1Api
        from sailpoint.auth_profile_v1.api_client import ApiClient
        from sailpoint.auth_profile_v1.models.authprofile import Authprofile
        from sailpoint.auth_profile_v1.models.jsonpatchoperation import Jsonpatchoperation
        from sailpoint.configuration import Configuration
        configuration = Configuration()

        configuration.experimental = True

        with ApiClient(configuration) as api_client:
          id = '2c91808a7813090a017814121919ecca' # str | ID of the Auth Profile to patch. # str | ID of the Auth Profile to patch.
          jsonpatchoperation = '''[sailpoint.auth_profile_v1.Jsonpatchoperation()]''' # List[Jsonpatchoperation] | 
          x_sail_point_experimental = 'true' # str | Use this header to enable this experimental API. (optional) (default to 'true') # str | Use this header to enable this experimental API. (optional) (default to 'true')
          try:
              # Patch a specified auth profile
              new_jsonpatchoperation = Jsonpatchoperation.from_json(jsonpatchoperation)
              results = AuthProfileV1Api(api_client).patch_profile_config_v1(id=id, jsonpatchoperation=new_jsonpatchoperation)
              # Below is a request that includes all optional parameters
              # results = AuthProfileV1Api(api_client).patch_profile_config_v1(id, new_jsonpatchoperation, x_sail_point_experimental)
              print("The response of AuthProfileV1Api->patch_profile_config_v1:\n")
              print(results.model_dump_json(by_alias=True, indent=4))
          except Exception as e:
              print("Exception when calling AuthProfileV1Api->patch_profile_config_v1: %s\n" % e)

